<?php

function b_stats_module_menu() {
  $items = array();

  $items['stats'] = array( // рут с формой
    'title' => 'Статистика заявок',
    'page callback' => array('drupal_get_form'),
    'page arguments' => array('b_stats_module_form'),
    'access arguments' => array('access content'),
  );

  return $items;
}

function b_stats_module_form($form, &$form_state){
    $form['text_field'] = array( 
    '#title' => 'Тест',
    '#type' => 'fieldset',
    '#description' => 'Тестовый Текст',
  );

  return $form;
}